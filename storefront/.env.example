# API Configuration
# Client-side API URL (exposed to browser)
PUBLIC_API_URL=http://localhost:8080/api/v1

# Server-side API URL (used in SSR, not exposed to browser)
# For Docker: use container name, e.g., http://app_monolith:8080/api/v1
INTERNAL_API_URL=http://localhost:8080/api/v1

# Midtrans Configuration
# Client key for payment integration (exposed to browser)
PUBLIC_MIDTRANS_CLIENT_KEY=SB-Mid-client-XXXX
# Midtrans environment: sandbox or production
PUBLIC_MIDTRANS_ENVIRONMENT=sandbox
